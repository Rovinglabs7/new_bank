# Active backends — initial blue slots (deploy.sh rewrites on cutover)
upstream new-bank_web {
    server 127.0.0.1:8004;
    keepalive 32;
}

upstream new-bank_api {
    server 127.0.0.1:8006;
    keepalive 32;
}
