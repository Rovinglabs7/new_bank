import { RampSection } from "@/components/ramp/RampSection";

/** Platform hero — back office + agents (includes Lottie visuals) */
export function RampPlatformBackOffice() {
  return <RampSection slug="platform-back-office" />;
}
